extends layout

block content
  div (class="container")
      h1 Registered Users
      ul(class="userlist")
      each user, i in users
        li
          a(href="mailto:#{user.email}")=user.username
          span - #{user.email}
