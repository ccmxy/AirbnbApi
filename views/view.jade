extends layout

block content
  div(class="container")
      ul(class="userlist")
      each user, i in users
        li
          h1 #{user.username}
          a(href="mailto:#{user.email}")=user.email

       a(href='/') Home
